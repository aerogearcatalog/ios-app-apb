version: 1.0
name: ios-app-apb
description: Sets up an iOS App representation.
bindable: False
async: optional
tags: 
  - mobile
metadata:
  displayName: iOS App
  console.openshift.io/iconClass: fa fa-apple
plans:
  - name: default
    description: Sets up an iOS App representation.
    free: True
    metadata: {}
    parameters: 
    - name: appName
      required: True
      default: myapp
      type: string
      title: Application Name
    - name: appIdentiifer
      required: True
      type: string
      title: Bundle ID